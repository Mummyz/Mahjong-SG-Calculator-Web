/**
 * Set decomposition — enumerate every legal way a hand splits into
 * melds/pairs/special shapes.
 *
 * Run 1 fills this in. Scoring must consider ALL decompositions and take the
 * best-scoring one, not the first one found — a hand that parses two ways is
 * scored at its maximum.
 */
export {}
