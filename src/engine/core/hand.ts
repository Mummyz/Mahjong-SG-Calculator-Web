/**
 * Hand parser — turns a keyed-in tile selection (full or partial) into a
 * validated hand structure the scorer and the predictor can both consume.
 *
 * Run 1 fills this in. Partial hands are first-class: the Run 4 predictor
 * depends on this accepting an incomplete tile set without throwing.
 */
export {}
