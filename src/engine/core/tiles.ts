/**
 * Tile model — the shared vocabulary every variant is expressed in.
 *
 * Run 1 fills this in. It must cover the union of both FINAL variants:
 *   - Hong Kong Old Style: 144 tiles (suits, honours, flowers/seasons)
 *   - Singapore:           148 tiles (the 144 + 4 animals)
 * A variant declares which of these it actually uses; nothing here is
 * variant-specific.
 */
export {}
